# === SMTP / Gmail settings ===
SMTP_SERVER=smtp.gmail.com
SMTP_PORT=587
SMTP_USERNAME=jjburr02@gmail.com
SMTP_PASSWORD=ytptywezqsictvcx

# === App behavior ===
TIMEZONE=America/Denver
POLL_SECONDS=600         # seconds between checks
JITTER_SECONDS=30        # +/- random seconds to avoid exact polling times

# === Target dates (optional override) ===
# Comma-separated list YYYY-MM-DD
# If blank, the script defaults to the next Saturday and Sunday
# TARGET_DATES=2025-08-16,2025-08-17
